import 'package:flutter/material.dart';

class EmailUpdateView extends StatelessWidget {
  const EmailUpdateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Email Adresi Güncellenemez"),
      ),
    );
  }
}
