import 'package:flutter/material.dart';

class ServiceCategorysView extends StatelessWidget {
  const ServiceCategorysView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Hizmet Kategorileri"),
      ),
    );
  }
}
